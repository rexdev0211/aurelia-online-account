//import {bindable} from 'aurelia-framework';
import {BasePage} from './../../../bases/base-page';

export class CreateWebhookPage extends BasePage {

  constructor(...args) {
    super(...args);
  }
}

